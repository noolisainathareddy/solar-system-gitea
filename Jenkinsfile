pipeline{
    agent any
    tool {
        node 'nodejs21'
    }
    stages {
        stage('first stage') {
            steps{
                sh 'sudo apt install nodejs'
            }
        }
    }
}